{
  "name": "branch-locator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "live-server": "^1.2.1",
    "pug": "^3.0.2",
    "pug-cli": "^1.0.0-alpha6",
    "typescript": "^4.2.4"
  },
  "devDependencies": {},
  "scripts": {
    "dev": "npx tsc -w & pug src/pug --out build -w & npm run serve",
    "build": "npx tsc -b && pug src/pug --out build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "live-server build --watch"
  },
  "author": "",
  "license": "ISC"
}
